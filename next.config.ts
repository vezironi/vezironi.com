import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["cdn.discordapp.com", "i.scdn.co", "raw.githubusercontent.com", "cdn.simpleicons.org"],
  },
};

export default nextConfig;
